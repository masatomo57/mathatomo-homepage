/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // Change to enable static site generation (SSG)
    assetPrefix: './', // Set the asset prefix for static files
};

export default nextConfig;
